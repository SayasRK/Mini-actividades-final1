using UnityEngine;

public class E12 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Creamos un bucle 
        //Con este "for" mostramos los números del 1 al 100 uno por uno en consola. 

        for (int i = 1; i <= 100; i++)
        {
            Debug.Log(i);
        }

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
